CAPI=2:
name : pulp-platform.org::tech_cells:0.2.13

filesets:
  rtl:
    files:
      - pulpplatform_tech_cells/src/rtl/tc_sram.sv
      - pulpplatform_tech_cells/src/rtl/tc_clk.sv
    file_type : systemVerilogSource

targets:
  default:
    filesets:
    - "!target_format ? (!target_lint ? (rtl))"
