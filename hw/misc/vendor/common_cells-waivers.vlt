`verilator_config

lint_off -rule ASSIGNIN -file "*/*common_cells*/src/fifo_v3.sv"     -match "Assigning to input/const variable: 'clk_i'"
lint_off -rule CMPCONST -file "*/*fifo_v2.sv"                       -match "Comparison is constant due to limited range"
lint_off -rule UNSIGNED -file "*/*common_cells*/src/rr_arb_tree.sv" -match "Comparison is constant due to unsigned arithmetic"

// They aren't used - just ignore
lint_off -rule REDEFMACRO -file "*/prim_assert_dummy_macros.svh" -match "*"
lint_off -rule REDEFMACRO -file "*/prim_assert.sv" -match "*"
