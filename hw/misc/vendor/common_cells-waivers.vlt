
`verilator_config
lint_off -rule PINCONNECTEMPTY -file "*/common_cells*/*.sv"
lint_off -rule DECLFILENAME    -file "*/common_cells*/*.sv" -match "Filename '*' does not match IFACE name: '*'"
lint_off -rule DECLFILENAME    -file "*/common_cells*/*.sv" -match "Filename '*' does not match MODULE name: '*'"
lint_off -rule UNUSED          -file "*/common_cells*/src/fifo_v3.sv" -match "Signal is not used: 'testmode_i'"
// Lint on unused common cells(?)
// stream_xbar
// stream_demux
//lint_off -rule WIDTH           -file "*/common_cells*/src/*" -match "Operator VAR '*' expects 32 bits on the Initial value, but Initial value's CONST '*' generates 1 bits."
//lint_off -rule WIDTH           -file "*/common_cells*/src/*" -match "Operator COND expects 32 bits on the Conditional False, but Conditional False's CONST '*' generates 1 bits."
//lint_off -rule WIDTH           -file "*/common_cells*/src/*" -match  "Operator VAR 'ExtPrio' expects 1 bits on the Initial value, but Initial value's CONST '32'h0' generates 32 bits."


// clk gating
lint_off -rule ASSIGNIN        -file "*/common_cells*/src/fifo_v3.sv" -match "Assigning to input/const variable: 'clk_i'"

lint_off -rule CMPCONST -file "*/fifo_v2.sv" -match "Comparison is constant due to limited range"


lint_off -rule UNUSED -file   "*/stream_fifo_optimal_wrap.sv" -match "Signal is not used: 'testmode_i'"
lint_off -rule UNUSED -file   "*/passthrough_stream_fifo.sv" -match "Signal is not used: 'testmode_i'"
lint_off -rule UNUSED -file   "*/passthrough_stream_fifo.sv" -match "Parameter is not used: 'PrintInfo'"
lint_off -rule UNUSED -file   "*/shift_reg_gated.sv" -match "Signal is not used: '*'"
lint_off -rule UNUSED -file   "*/cdc_fifo_gray.sv" -match "Signal is not driven, nor used: '*'"
lint_off -rule UNUSED -file   "*/cdc_fifo_gray.sv" -match "Parameter is not used: '*'"
lint_off -rule UNUSED -file   "*/cdc_reset_ctrlr.sv" -match "Parameter is not used: '*'"

