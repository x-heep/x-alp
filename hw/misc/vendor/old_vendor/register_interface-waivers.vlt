`verilator_config
lint_off -rule PINCONNECTEMPTY -file "*/register_interface*/*.sv"
lint_off -rule DECLFILENAME    -file "*/register_interface*/*.sv" -match "Filename '*' does not match IFACE name: '*'"
lint_off -rule DECLFILENAME    -file "*/register_interface*/*.sv" -match "Filename '*' does not match MODULE name: '*'"

// BUG to me, related to iDMA wrap
lint_off -rule WIDTH -file "*/reg_mux.sv" -match "Operator ASSIGNW expects 64 bits on the Assign RHS, but Assign RHS's SEL generates 32 bits."
lint_off -rule WIDTH -file "*/reg_mux.sv" -match "Operator ASSIGNW expects 32 bits on the Assign RHS, but Assign RHS's SEL generates 64 bits."
lint_off -rule WIDTH -file "*/reg_mux.sv" -match "Operator ASSIGNW expects 8 bits on the Assign RHS, but Assign RHS's SEL generates 4 bits."
lint_off -rule WIDTH -file "*/reg_mux.sv" -match "Operator ASSIGNW expects 4 bits on the Assign RHS, but Assign RHS's SEL generates 8 bits."
lint_off -rule WIDTH -file "*/periph_to_reg.sv" -match "Operator ASSIGNW expects 64 bits on the Assign RHS, but Assign RHS's * generates 32 bits."
lint_off -rule WIDTH -file "*/periph_to_reg.sv" -match "Operator ASSIGN expects 32 bits on the Assign RHS, but Assign RHS's SEL generates 64 bits."
lint_off -rule WIDTH -file "*/periph_to_reg.sv" -match "Operator ASSIGNW expects 8 bits on the Assign RHS, but Assign RHS's * generates 4 bits."

// USB internal axi reduced
lint_off -rule WIDTH -file "*/reg_to_axi.sv" -match "Operator PATMEMBER expects 3 bits on the Pattern value, but Pattern value's CLOG2 generates 32 bits."
lint_off -rule WIDTH -file "*/reg_to_axi.sv" -match "Operator PATMEMBER expects 12 bits on the Pattern value, but Pattern value's SEL generates 48 bits."

