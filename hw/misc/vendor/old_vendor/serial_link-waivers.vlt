`verilator_config
lint_off -rule WIDTH -file "*/serial_link_data_link.sv" -match "Operator * expects * bits on the *, but * generates * bits."
lint_off -rule WIDTH -file "*/serial_link_network.sv" -match "Operator * expects * bits on the *, but * generates * bits."

lint_off -rule UNOPTFLAT -file "*/serial_link_network.sv" -match "Signal unoptimizable: Feedback to clock or circular logic: *__Vcellout__i_serial_link_network__axi_in_rsp_o'"

lint_off -rule PINCONNECTEMPTY -file "*/serial_link_reg_top.sv"
lint_off -rule PINCONNECTEMPTY -file "*/serial_link_single_channel_reg_top.sv"

lint_off -rule DECLFILENAME -file "*/serial_link_reg_top.sv" -match "Filename * does not match MODULE name: '*'"
lint_off -rule DECLFILENAME -file "*/serial_link_single_channel_reg_top.sv" -match "Filename * does not match MODULE name: '*'"
lint_off -rule DECLFILENAME -file "*/serial_link_physical.sv" -match "Filename * does not match MODULE name: '*'"


lint_off -rule PINCONNECTEMPTY -file "*/serial_link_physical.sv" -match "* pin connected by name with empty reference: 'src_ready_o'"
lint_off -rule PINCONNECTEMPTY -file "*/serial_link_network.sv" -match "* pin connected by name with empty reference: 'usage_o'"
lint_off -rule PINCONNECTEMPTY -file "*/serial_link_data_link.sv" -match "* pin connected by name with empty reference: 'usage_o'"


lint_off -rule VARHIDDEN -file "*/serial_link.sv"
lint_off -rule VARHIDDEN -file "*/serial_link_data_link.sv"
lint_off -rule VARHIDDEN -file "*/serial_link_channel_allocator.sv"

lint_off -rule BLKSEQ -file "*/serial_link_physical.sv"
//-match "Blocking assignments (=) in sequential (flop or latch) block"

lint_off -rule UNOPTFLAT -file "*/serial_link.sv" -match "Signal unoptimizable: Feedback to clock or circular logic: *.phy2alloc_data_out_ready'"


